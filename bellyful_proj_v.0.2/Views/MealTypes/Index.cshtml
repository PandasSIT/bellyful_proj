@model IEnumerable<bellyful_proj.Models.MealType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MealTypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShelfLocation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MealTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShelfLocation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MealTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MealTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MealTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
